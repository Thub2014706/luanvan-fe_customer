import React from 'react';
import BookBar from '~/components/BookBar/BookBar';

const BookNow = () => {
    return (
        <div>
            <BookBar />
        </div>
    );
};

export default BookNow;
